import { defineMermaidSetup } from '@slidev/types'

export default defineMermaidSetup(() => {
  return {
    themeVariables: {
      mainBkg: '#fff',
      nodeBorder: '#fff',
      edgeLabelBackground: '#fb7a9c',
      textColor: '#000'
    }
  }
})
